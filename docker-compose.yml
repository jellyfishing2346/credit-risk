services:
  api:
    build: .
    ports:
      - "8000:8000"
    # Clear the Lambda entrypoint so uvicorn runs directly for local testing
    entrypoint: []
    command: ["python", "-m", "uvicorn", "credit_risk.api.main:app", "--host", "0.0.0.0", "--port", "8000"]
